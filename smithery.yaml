startCommand:
  type: http
  commandFunction:
    |-
    (config) => ({ command: 'node', args: ['src/index.js'] })
  configSchema:
    type: object
    properties: {}
    description: No configuration required
  exampleConfig: {}